// Optional: Track clicks and downloads
function trackDownload(item) {
    console.log(`Downloaded: ${item}`);
    // Add analytics (e.g., Google Analytics) here if desired
}

function trackClick(action) {
    console.log(`Clicked: ${action}`);
    // Add analytics here if desired
}